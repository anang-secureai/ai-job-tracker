# ── Required ──────────────────────────────────────────────────────────
# PostgreSQL connection string (Railway sets this automatically when you
# add a Postgres database to your project)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Admin panel password (choose something strong)
ADMIN_PASSWORD=your-secure-admin-password

# ── Optional ──────────────────────────────────────────────────────────
# Session signing secret — Railway will auto-generate one if omitted,
# but sessions won't survive restarts without a fixed value
SESSION_SECRET=random-secret-string-at-least-32-chars

# Port — Railway sets this automatically, no need to change
# PORT=3000
